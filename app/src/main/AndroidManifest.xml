<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WellSync">


        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.WellSync.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash Activity (Disabled temporarily) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.WellSync.NoActionBar" />

        <!-- Recovery Activity (Data Corruption Recovery) -->
        <activity
            android:name=".activities.RecoveryActivity"
            android:exported="false"
            android:theme="@style/Theme.WellSync.NoActionBar" />

        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.WellSync.NoActionBar" />

        <!-- Main Activity (Bottom Navigation) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.WellSync.NoActionBar" />

        <!-- Water Limit Activity -->
        <activity
            android:name=".activities.WaterLimitActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/Theme.WellSync" />

        <!-- WellSync Widget Provider -->
        <receiver
            android:name=".widgets.WellSyncWidgetProvider"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/wellsync_widget_info" />
        </receiver>

        <!-- Notification Receiver -->
        <receiver
            android:name=".utils.NotificationUtils$NotificationReceiver"
            android:exported="false" />

    </application>

</manifest>